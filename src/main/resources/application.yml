spring:
  profiles:
    active: prod

  application:
    name: notepad

  messages:
    basename: messages
    encoding: UTF-8

logging:
  level:
    org:
      hibernate:
        sql: info
        orm:
          jdbc:
            bind: info
